	# ~ Chapters=dict()
		# ~ for i in self.Chapters:
			# ~ MainText=self.Chapters[i].MainText
			# ~ Answers=self.Chapters[i].Answers
			# ~ Options=self.Chapters[i].Options
			# ~ Results=self.Chapters[i].Results
			# ~ Chapters[i]={'MainText' : MainText,'Answers' : Answers,'Options' : Options,'Results' : Results}
		# ~ storyfile = open(StoryDirPointer+'story.json','w')
		# ~ storyfile.write(json.dumps(Chapters))
		# ~ storyfile.close()
